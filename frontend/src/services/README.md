# Services

Frontend services isolate API and integration details from React components.

- `api`: FastAPI client modules.
- `storage`: Supabase Storage client helpers.
- `graph`: React Flow graph data adapters.
