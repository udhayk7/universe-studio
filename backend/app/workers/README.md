# Workers

Long-running job runners and background task adapters live here.

For the hackathon, FastAPI background tasks or a simple async job service are enough. Add Celery, Dramatiq, or another queue only if the demo genuinely needs it.
